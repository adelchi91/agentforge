# Lagrangia MecAI — Claude Code Constitution

> Generated by claude-project-bootstrap v1.0.0
> Edit this file to reflect project-specific rules.

## Golden Rule

The E2E test suite must never break during migration. If any E2E test fails at the
end of a phase, the phase is rolled back and the story is reopened.

## Project Overview

Lagrangia MecAI is a CAD code generation system built for Dessia Technologies. It uses
LangGraph for multi-agent orchestration, RAG pipelines backed by ChromaDB for contextual
knowledge retrieval, and MCP servers to expose mechanical engineering tools to AI agents.
The system generates Python code for mechanical component design, validated against
Dessia's internal CAD framework.

**Stack:** Python, LangGraph, ChromaDB, FastAPI

## Phase Map

Phase 1 — Extract dessia-memory: Isolate ChromaDB memory into standalone package (STORY-001)
Phase 2 — Extract dessia-tools + YAML contract: Tool server extraction + service contract (STORY-002 – STORY-003)
Phase 3 — Code Explorer library: Refactor into standalone callable library with public API (STORY-004 – STORY-006)
Phase 4 — dessia-multiagents Supervisor: LangGraph supervisor + agent coordination (STORY-007 – STORY-012)
Phase 5 — dessiaworker refactor + CI/CD: Final integration, dead-code removal, multi-repo CI (STORY-013 – STORY-016)

## Repo Ownership

- architect-multiagents → dessia-multiagents/, service_contract.yaml
- architect-code-explorer → code-explorer/
- architect-dessiaworker → dessiaworker/ (read-only; ADRs and stories only)
- dev-memory → dessia-memory/
- dev-tools → dessia-tools/
- tester → full repo (read + bash; no write)
- final-judge → full repo (read only; approval authority)

## Hard Constraints

- No breaking changes to the `dessiaworker` public API at any point during migration
- The CI pipeline must remain green at the end of every story (no red CI merges)
- No files in the monorepo are deleted until Phase 5 is fully validated and signed off by final-judge
- All git commits must reference a STORY-XXX number in the commit message

## Common Failure Modes

<!-- Fill this in as you discover them. Examples:
- Agent touched files outside its scope → tighten allowed-tools in agent file
- Tester reported PASS but E2E failed → add E2E command to verification commands in story
- Story scope was too large → split into two stories
- Cross-package import leaked into extracted package → add isolation grep to verification
-->

## Out of Scope for All Agents

- Deleting files from the monorepo before Phase 5 is validated
- Modifying files not listed in the agent's scope
- Merging any PR without final-judge approval
- Pushing with --force to any branch
