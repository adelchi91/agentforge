# Project Context — mylib

> Generated by claude-project-bootstrap v1.0.0 — Step 1 output.

## Identity

**Project name:** mylib

**Description:**
mylib is a Python utility library for data validation. It provides composable, type-safe
validators for common data types — strings, numbers, dates, collections — with a clean
functional API. It is designed to be used as a dependency in data-processing pipelines
where input validation must be explicit and testable.

**Stack:** Python 3.11+, pytest, ruff

## Codebase

**Existing code:** No (greenfield)

**Main deliverable:** A published Python package on PyPI with 100% test coverage and
a stable public API suitable for use as a library dependency.

## Constraints

**Hard constraints:** None specified beyond the golden rule.

**Golden rule:** All tests must pass before merging. No PR may be merged with failing
or skipped tests.
