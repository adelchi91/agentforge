# calc-kit — Codex Project Constitution

> Generated by project-bootstrap v1.1.0 for Codex

## Golden Rule

The test suite must never break during development.

## Project Overview

calc-kit is a small Python arithmetic library with a CLI wrapper for simple calculations.

**Stack:** Python 3.11+, uv, pytest

## Phase Map

- Phase 1 — Foundation: package skeleton and baseline tests (STORY-001)
- Phase 2 — Calculator Engine: parser and evaluator implementation (STORY-002 – STORY-004)

## Repo Ownership

- final-judge → full repo read-only
- dev → `src/`, `tests/`, `pyproject.toml`
- tester → read-only plus test commands

## Hard Constraints

- Keep the test suite green after each story.
- Do not add runtime dependencies unless a story explicitly approves them.

## Common Failure Modes

<!-- Fill this in as the project runs. -->

## Out of Scope for All Agents

- Modifying files outside the assigned story scope
- Merging any PR without final-judge approval

## Codex Workflow Rules

- Use `.agents/stories/STORY-XXX.md` as the source of truth for assigned work.
- Show a checkpoint before file modifications and wait for `GO`.
- Reference the story ID in every git commit message.
- Run every verification command listed in the story.
