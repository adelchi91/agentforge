{
  "description": "AgentForge lifecycle hooks. PreToolUse wires the STORY-013 graded command-policy engine (scripts/scope_policy.py); UserPromptSubmit wires the STORY-010 named-work-context injector (scripts/context.py); SessionStart (STORY-009) is added later. Scripts are always invoked via ${CLAUDE_PLUGIN_ROOT}/scripts/.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Write|Edit|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/scope_policy.py\""
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/context.py\" user-prompt-submit"
          }
        ]
      }
    ]
  }
}
