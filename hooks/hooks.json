{
  "description": "AgentForge lifecycle hooks. PreToolUse wires the STORY-013 graded command-policy engine (scripts/scope_policy.py); SessionStart (STORY-009) and UserPromptSubmit (STORY-010) entries are added later. Scripts are always invoked via ${CLAUDE_PLUGIN_ROOT}/scripts/.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Write|Edit|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/scope_policy.py\""
          }
        ]
      }
    ]
  }
}
