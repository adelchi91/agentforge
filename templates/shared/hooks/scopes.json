{
  "_readme": "Generated by project-bootstrap from the Repo Ownership table. Maps each agent name to the path prefixes it may Write/Edit. Agents listed here with an empty allow-list are write-blocked entirely (read-only). Agents NOT listed (and the main session) are not scope-restricted by the hook. Paths are relative to the project root; a trailing slash means the whole subtree.",
  "agents": {
    "{{AGENT_NAME}}": {
      "allow": [
        "{{ALLOWED_PATH_PREFIX}}"
      ]
    }
  }
}
