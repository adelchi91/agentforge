---
type: tool_used
tool: Skill
input_match: "reconcile-docs"
min: 1
target: trace
---
The scenario matches reconcile-docs's description (reference PRD/ADR plus
a proposed spec to check against them), so the skill should load. Marked
as an ablation ("with"-only) indicator by the eval runner rather than a
hard pass/fail gate.
