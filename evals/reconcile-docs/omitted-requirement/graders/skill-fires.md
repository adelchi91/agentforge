---
type: tool_used
tool: Skill
input_match: "reconcile-docs"
min: 1
target: trace
---
The scenario matches reconcile-docs's description (reference PRD plus a
proposed ticket graph to check coverage against), so the skill should load.
Marked as an ablation ("with"-only) indicator by the eval runner rather
than a hard pass/fail gate.
