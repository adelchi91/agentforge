---
type: llm
criteria: "The response's acceptance criteria describe observable outcomes a person with no knowledge of the fix could confirm -- e.g. checking out an empty cart no longer returns a 500 response and instead shows a specific friendly message -- rather than restating the task ('fix the bug') or describing an implementation step ('add a null check')."
target: last_message
---
STORY-007 requires acceptance criteria to be observable outcomes, not
task restatements or implementation steps.
